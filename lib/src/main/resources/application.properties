#information for connecting to the database
spring.datasource.url=jdbc:mysql://localhost:3306/evaluation_img?useSSL=false
spring.datasource.username=root
spring.datasource.password=192837465Aaa


#Means that when the server is run, the database instance is updated. Another value: create, create-drop, validate and none.
spring.jpa.hibernate.ddl-auto=update

#show the hibernation-generated SQL in the console
spring.jpa.show-sql=true

#mentions which database will be used
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect

#disable the OSIV(Open Session in View). If it is enable OSIV affects the connection and performance of the database because OSIV
# keeps the connection up to the UI level, which increases the time to rent the connection to the database.
spring.jpa.open-in-view=false

